# Secrets — never commit
.env
.env.*
!.env.example
# Google service account key (used for the Sheets integration)
service-account*.json
*service-account*.json
*.gserviceaccount.json

# Local SQLite database (contains password hashes and candidate PII)
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Dependencies / build output
node_modules
dist
dist-ssr

# OS / editor / FS junk
.DS_Store
.fuse_hidden*
.idea
*.log
